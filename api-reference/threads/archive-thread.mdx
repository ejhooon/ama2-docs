---
title: "Archive Thread"
api: "PATCH /api/v1/chat/threads/{thread_id}/archive"
description: "Archive a thread."
---

## Archive Thread

Archive a thread. Archived threads are hidden from the default thread list but not deleted.

### Authentication

<Note>Requires a valid user JWT token.</Note>

### Path Parameters

<ParamField path="thread_id" type="string" required>
  UUID of the thread to archive.
</ParamField>

### Example

```bash
curl -X PATCH https://api.ama2.me/api/v1/chat/threads/{thread_id}/archive \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

```json
{
  "status": "ok"
}
```
